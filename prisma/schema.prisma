generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Profile {
  name      String   @id
  data      Json
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("profiles")
}
