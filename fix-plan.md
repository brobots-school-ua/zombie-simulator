# Fix Plan — Round 3

## 1. Зменшити кількість куль з пікапу
**Problem:** Гравець отримує забагато куль за раз (15 в запас).
**Fix:** Зменшити `value` для ammo pickup з 15 до 5-7 куль.
**Files:** `src/entities/Pickup.ts`

## 2. Zombie spawn rates — більше танків і раннерів
**Problem:** Танки майже не з'являються, раннерів мало.
**Fix:** Змінити ймовірності:
- Wave 1-2: 100% walkers (як зараз)
- Wave 3+: 60% walker, 30% runner, 10% tank
- Wave 5+: 40% walker, 35% runner, 25% tank
- Wave 8+: 30% walker, 35% runner, 35% tank
**Files:** `src/scenes/GameScene.ts`

## 3. HP bar для зомбі
**Problem:** Гравець не бачить скільки HP залишилось у зомбі.
**Fix:** Додати маленький HP bar над кожним зомбі (зелений/жовтий/червоний залежно від %). Малювати через `Graphics` об'єкт прив'язаний до зомбі.
**Files:** `src/entities/Zombie.ts`

## 4. Красивіші текстури — трава
**Problem:** Трава — просто зелений квадрат.
**Fix:** Генерувати текстуру трави з варіаціями кольору, крапками різних відтінків зеленого, маленькими травинками. Додати 2-3 варіанти тайлів для різноманітності.
**Files:** `src/scenes/BootScene.ts`, `src/scenes/GameScene.ts`

## 5. Красивіші текстури — каміння замість стін
**Problem:** Стіни — сірі квадрати без деталей.
**Fix:** Замінити текстуру `wall` на каміння: сірі/коричневі відтінки з нерівностями, тріщинами, тінями. Зробити вигляд натурального каменю.
**Files:** `src/scenes/BootScene.ts`

---

# Fix Plan — Round 4

## 6. Повноекранна гра
**Problem:** Гра була фіксована 800x600.
**Fix:** Змінити scale mode на `RESIZE`, розмір вікна = `window.innerWidth/Height`. UI адаптується до розміру екрану.
**Files:** `src/main.ts`, `src/scenes/UIScene.ts`
**Status:** ✅ Done

## 7. Ремодель гравця — куля замість куба
**Problem:** Гравець виглядав як квадрат.
**Fix:** Новий спрайт — коло з 3D-ефектом (градієнт, highlight). Хітбокс змінено на круглий (`setCircle`).
**Files:** `src/scenes/BootScene.ts`, `src/entities/Player.ts`
**Status:** ✅ Done

## 8. Зброя обертається окремо від тіла
**Problem:** Весь спрайт гравця обертався за мишкою.
**Fix:** Додано окремий спрайт `weapon`, який прив'язаний до позиції гравця але обертається незалежно за курсором. Тіло гравця (куля) не обертається.
**Files:** `src/entities/Player.ts`, `src/scenes/BootScene.ts`
**Status:** ✅ Done

## 9. Кулі вилітають з дула зброї
**Problem:** Кулі спавнились з центру гравця.
**Fix:** Додано метод `getMuzzlePosition()` — обчислює позицію кінця зброї. Кулі тепер створюються з цієї точки.
**Files:** `src/entities/Player.ts`, `src/scenes/GameScene.ts`
**Status:** ✅ Done

---

# Fix Plan — Round 5

## 10. Камера зміщена вліво — центрувати
**Problem:** Екран зміщений вліво, гравець не по центру.
**Fix:** Змінити scale mode з `RESIZE` на `FIT` з фіксованим базовим розміром 1280x720 + `autoCenter: CENTER_BOTH`. Canvas буде по центру на будь-якому екрані.
**Files:** `src/main.ts`

## 11. Камера занадто далеко — не показувати всю мапу
**Problem:** Видно майже всю карту, зум занадто далекий.
**Fix:** Збільшити зум камери (`setZoom(1.5)`), щоб гравець бачив лише область навколо себе. Це створить кращу атмосферу і напругу.
**Files:** `src/scenes/GameScene.ts`

## 12. Мінімапа
**Problem:** При збільшеному зумі гравець не бачить де він на карті.
**Fix:** Додати мінімапу в кут екрану:
- Маленький прямокутник (180x180) у правому нижньому куті
- Показує всю карту зменшеною
- Зелена точка — гравець
- Червоні точки — зомбі
- Сірі квадрати — стіни
- Жовті точки — ammo pickups
- Малювати через `Graphics` в UIScene, оновлювати кожен кадр
**Files:** `src/scenes/UIScene.ts`

## 13. Покращити стартове меню (поради Gemini)
**Problem:** Меню просте — тільки текст.
**Fix:** Оновити MenuScene:
- Темний фон з рухомими напівпрозорими колами (імітація туману/тіней)
- Заголовок "ZOMBIE SIMULATOR" з ефектом миготіння (як зламана неонова вивіска) — випадкове alpha мерехтіння
- Декоративні зомбі-силуети (зелені кола), що повільно пересуваються на фоні
- Кнопка START з hover-ефектом (збільшення scale + glow)
- Підказки керування з плавною появою (fade in з затримкою)
- Кольорова схема: чорний фон, червоний заголовок, зелений акцент
**Files:** `src/scenes/MenuScene.ts`

## 14. Покращити текстури зомбі (поради Gemini)
**Problem:** Зомбі — прості квадрати з очима.
**Fix:**
- Walker: додати "рвані" краї, червоні плями крові (fillCircle), витягнуту руку вперед
- Runner: худіший силует, подряпини, динамічна поза
- Tank: масивніший з "бронею", шрами, більш деталізований
**Files:** `src/scenes/BootScene.ts`

## 15. Покращити текстуру куль (поради Gemini)
**Problem:** Кулі — просте жовте коло, нудно.
**Fix:**
- Змінити форму на витягнуту капсулу/еліпс (трасер)
- Яскраве ядро (білий центр) + напівпрозоре сяйво навколо
- Додати particle trail за кулею (швидкозатухаючі частинки що слідують за bullet)
**Files:** `src/scenes/BootScene.ts`, `src/scenes/GameScene.ts`

## 16. Покращити текстуру гравця (поради Gemini)
**Problem:** Гравець — просто синя куля.
**Fix:**
- Додати "плечі" (два менших кола по боках тіла)
- Top-down shooter стиль (як у Crimsonland)
- Детальніший 3D-ефект на тілі
**Files:** `src/scenes/BootScene.ts`
